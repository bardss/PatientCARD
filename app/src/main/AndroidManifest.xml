<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jakubaniola.patientcard">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application
        android:name="com.example.jakubaniola.patientcard.base.ApplicationContext"
        android:allowBackup="true"
        android:icon="@drawable/medical_history"
        android:label="@string/app_name"
        android:roundIcon="@drawable/medical_history"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".qrreader.QRReaderActivity"/>
        <activity android:name=".access.AccessActivity"/>
        <activity android:name=".demographic.DemographicActivity"/>
    </application>

</manifest>